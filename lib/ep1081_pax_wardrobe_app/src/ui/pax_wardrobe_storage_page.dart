import 'package:flutter/material.dart';

class PaxWardrobeStoragePage extends StatelessWidget {
  const PaxWardrobeStoragePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Storage UI "),
    );
  }
}
