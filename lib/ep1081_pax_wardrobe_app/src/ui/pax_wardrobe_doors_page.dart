import 'package:flutter/material.dart';

class PaxWardrobeDoorsPage extends StatelessWidget {
  const PaxWardrobeDoorsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Door UI "),
    );
  }
}
